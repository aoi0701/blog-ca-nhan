---
title: "JavaScript State & UI – Vì sao Web phức tạp?"
date: 2025-12-29
weight: 6
description: "Giải thích khái niệm State trong JavaScript, mối quan hệ giữa State và UI, và vì sao xây dựng Web App trở nên phức tạp"
tags: ["JavaScript", "State", "UI", "Frontend"]
categories: ["Blog"]
series: ["JavaScript & Web"]
draft: false
---


### 1.1 UI tĩnh (HTML thuần)

```html
<h1>Hello</h1>
```

 Nội dung **không đổi**, không cần JavaScript.

---

### 1.2 UI động (JavaScript)

```html
<h1 id="count">0</h1>
<button id="btn">+</button>
```

```javascript
let count = 0;

btn.addEventListener("click", () => {
  count++;
  document.getElementById("count").innerText = count;
});
```

 `count` chính là **state**.

---

## 2. State là gì?

**State** là:

> Toàn bộ dữ liệu mô tả **trạng thái hiện tại** của ứng dụng.

Ví dụ:

* Số lượng sản phẩm trong giỏ hàng
* Người dùng đã đăng nhập hay chưa
* Danh sách bài viết đang hiển thị

---

## 3. UI = f(State) (công thức QUAN TRỌNG)

Trong Web hiện đại:

```
UI = f(State)
```

 UI **không tự tồn tại**, nó phản ánh state.

Ví dụ:

```javascript
let isLogin = false;

function render() {
  document.body.innerHTML = isLogin
    ? "<h1>Xin chào User</h1>"
    : "<button>Đăng nhập</button>";
}

render();
```

---

## 4. Vấn đề: State nằm rải rác khắp nơi 

Ví dụ **không tốt**:

```javascript
let count = 0;

btn1.addEventListener("click", () => {
  count++;
  h1.innerText = count;
});

btn2.addEventListener("click", () => {
  count--;
  h1.innerText = count;
});
```

 Khi ứng dụng lớn:

* Không biết state ở đâu
* Dễ sai UI
* Khó debug

---

## 5. Ví dụ: Counter App (state + UI rõ ràng)

### 5.1 Cách viết “thủ công”

```html
<h2 id="count">0</h2>
<button id="inc">+</button>
<button id="dec">-</button>
```

```javascript
let state = {
  count: 0
};

function render() {
  document.getElementById("count").innerText = state.count;
}

inc.addEventListener("click", () => {
  state.count++;
  render();
});

dec.addEventListener("click", () => {
  state.count--;
  render();
});

render();
```

 Đây là **nền tảng tư duy của React**.

---

## 6. State phức tạp hơn: List + UI

### 6.1 Todo list đơn giản

```html
<input id="task">
<button id="add">Add</button>
<ul id="list"></ul>
```

```javascript
const state = {
  todos: []
};

function render() {
  list.innerHTML = "";
  state.todos.forEach(todo => {
    const li = document.createElement("li");
    li.innerText = todo;
    list.appendChild(li);
  });
}

add.addEventListener("click", () => {
  state.todos.push(task.value);
  task.value = "";
  render();
});

render();
```

 Khi state thay đổi → UI update.

---

## 7. Vì sao Web trở nên phức tạp?

Khi app lớn lên:

* State nhiều
* Event nhiều
* UI phụ thuộc lẫn nhau

Ví dụ:

* Login → đổi menu
* Add cart → đổi badge
* Fetch data → loading / error / success

 **State chồng chéo = UI bug**.

---

## 8. State + Async = độ khó tăng gấp đôi 😨

```javascript
let state = { loading: true, data: null };

async function loadData() {
  const res = await fetch(url);
  state.data = await res.json();
  state.loading = false;
  render();
}
```

```javascript
function render() {
  if (state.loading) {
    document.body.innerHTML = "Loading...";
  } else {
    document.body.innerHTML = JSON.stringify(state.data);
  }
}
```

 Quản lý state async rất dễ lỗi.

---

## 9. So sánh với Java (để dễ hiểu)

| Java Desktop | JavaScript Web |
| ------------ | -------------- |
| Object       | State          |
| setText()    | render()       |
| MVC          | State + UI     |
| Listener     | Event          |

 Web **không khó**, chỉ là **quản lý state khó**.

---

## 10. Vì sao React / Vue ra đời?

Framework giải quyết:

* State tập trung
* UI tự động update
* Ít thao tác DOM thủ công

 Nhưng **hiểu state thuần JS trước** sẽ học framework rất nhanh.

---

## 11. Mini ví dụ: State-driven UI

```javascript
let state = { dark: false };

function render() {
  document.body.className = state.dark ? "dark" : "";
}

toggle.addEventListener("click", () => {
  state.dark = !state.dark;
  render();
});
```

 Đây chính là **tư duy hiện đại của Frontend**.

---

## 12. Sai lầm thường gặp

 Update DOM rải rác
 Không gom state
 UI và logic trộn lẫn
 Async làm loạn state

---

## 13. Tổng kết 

* **State là trung tâm của Web App**
* UI phản ánh state
* Web phức tạp vì state phức tạp
* Hiểu state → học React/Vue cực nhanh


